rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 보안을 잠시 포기하고 모든 로그인을 허용하여 동기화부터 성공시킵니다.
      allow read, write: if true;
    }
  }
}
