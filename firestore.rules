rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 로그인한 사용자만 데이터를 읽고 쓸 수 있도록 허용합니다.
      // 이렇게 하면 앱의 모든 데이터가 사용자 계정과 안전하게 연결됩니다.
      allow read, write: if request.auth != null;
    }
  }
}
